{
  "name": "better-teambuilder-for-showdown",
  "version": "1.0.0",
  "private": true,
  "description": "Adds custom filters, Pikalytics stats, Speed comparisons, a Defensive Profile, and Biggest Threats to the Showdown teambuilder.",
  "scripts": {
    "lint": "eslint src/",
    "lint:fix": "eslint --fix src/",
    "test": "vitest run",
    "prepare": "node -e \"try{require('child_process').execSync('git config core.hooksPath .githooks')}catch(e){}\""
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "eslint": "^9.27.0",
    "globals": "^16.1.0",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  },
  "overrides": {
    "lru-cache": "11.2.7"
  }
}
